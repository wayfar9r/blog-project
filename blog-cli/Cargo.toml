[package]
name = "blog-cli"
version = "0.1.0"
edition = "2024"

[dependencies]
blog-client = { version = "0.1.0", path = "../blog-client" }
chrono = "0"
clap = { version = "4", features = ["derive"] }
dotenvy = "0"
tokio = "1"
